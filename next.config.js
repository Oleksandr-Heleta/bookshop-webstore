/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'res.cloudinary.com',
            'mouse-admin.com.ua',
            'localhost',
        ]
    }
}

module.exports = nextConfig
